"Definitions of root types"
type Query {
  "Get genres"
  genres(
    page: Int
    itemsPage: Int
    active: ActiveFilterEnum = ACTIVE
  ): ResultGenres
  
  "Get genre by ID"
  genre(id: ID!): ResultGenre
}
